import sqlite3
import tkinter as tk

# TODO: Create Database
# TODO: Create GUI
# TODO: Create layout

def main():
    app = tk.Tk()
    app.title("Food Calendar")
    app.geometry("350x200")
    app.iconphoto(False, tk.PhotoImage(file="apple.ico"))
    info_label = tk.Label(app, text="Welcome to the Meal Planner", font=("Arial", :))
    info_label.pack(pady=20)
    info_label.config(text="Today's Breakfast: Pancakes")
    app.mainloop()

#if __name__ == "__main__":
#    main()
     
#class Fruit:
    # Constructor: Initializes the object's properties
    def __init__(self, name, color):
        self.name = name   # Attribute
        self.color = color # Attribute

    # Method: Behavior
    def describe(self):
        return f"This is a {self.color} {self.name}."

# Creating Instances (Objects)
#apple = Fruit("Apple", "red")
#blueberry = Fruit("Blueberry", "blue")

# Using the class
#print(apple.describe())     # Output: This is a red Apple.
#print(blueberry.color)      # Output: blue

#class Food:
    def __init__(self, name, category):
        self.name = name

    def info(self):
        return f"{self.name} is a type of {self.category}"
    
#
#fruit1 = Food("Apple", "Fruit")
#fruit2 = Food("Blueberry", "Fruit")
#starch1 = Food("Potato", "Starch")
#starch2 = Food("Yam", "Starch")

#print(fruit1.info()) #Output: Apple is a type of Fruit
#print(starch1.info()) #Output: Potato is a type of Starch
